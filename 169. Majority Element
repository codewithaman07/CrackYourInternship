class Solution {
public:
    int majorityElement(vector<int>& nums) { 
        int cnt = 0, ans = 0;
        for(auto &num : nums){
            if(cnt == 0) ans = num;
            if(num == ans) cnt++;
            else cnt--;
        }
        return ans;
    }
};
